

## Plan: Update phase 1 title

**File**: `src/components/demo/DemoPasswordGate.tsx`

Single text replacement — change `"Ventus Innovates the Entire Stack"` to `"Personalized Banking, Made Possible"` in the conditional heading that displays when `beat6Phase >= 1`.

