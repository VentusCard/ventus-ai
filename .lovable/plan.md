In `src/components/tepilot/insights/AnalyticsContainer.tsx`:

1. Reorder the **Home** group items so **System** (`capabilities`) is first, followed by Ventus AI, Products, Demo.
2. Change the `defaultTab` default from `'ventus-ai'` to `'capabilities'` so users land on the System tab when entering the page.

No other changes.