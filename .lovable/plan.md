Update the right-column header in `CapabilitiesView.tsx` from `Activation destinations · {DESTINATIONS.length} wired` to `{DESTINATIONS.length} Activation Destinations`, matching the format used for the sources column.

Technical detail: Replace line 773 text; `DESTINATIONS.length` is already computed and dynamic.