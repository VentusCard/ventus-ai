Two small visual adjustments to the three "Our" toggle buttons and the expanded content panel in `src/components/demo/SimplePasswordGate.tsx`:

1. **Shorter buttons** — Reduce the button height from `h-14` to `h-11` (or equivalent) so the collapsed titles are more compact.
2. **Larger expanded text** — Bump the expanded panel paragraph from `text-sm` to `text-base` (one step up in Tailwind's type scale).

No other layout or behavior changes.