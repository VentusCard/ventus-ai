Reorder `QueryConsoleView.tsx` so the AI "Refine" chip (Sparkles input + Generate button) renders directly **above** the pre-set example pills, instead of below the editor.

New order: Toolbar → AI chip → Example pills → Editor → Error → Results.

UI-only move, no logic or style changes.