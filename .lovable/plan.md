Change the gray background on the Platform Tabs analytics preview panel to white.

**What to change:**
- In `src/components/PlatformTabs.tsx`, line 482: change `bg-gray-50` to `bg-white` on the right-column preview container.

This makes the entire preview area match the white background of the rest of the page section.