In `src/components/tepilot/coworker-inbox/CoworkerInboxView.tsx`, reorder the 4 `KpiCard` components inside the `grid` block (currently lines 101-130) so that the "Signals surfaced" card appears first, followed by the remaining three cards in their existing relative order.

New order:
1. Signals surfaced
2. Emails sent this week
3. Human reply rate
4. Ventus reply latency