# Update collapsed KYC label

In `src/components/exec-demo/ExecDemoSelectionDialog.tsx` (line ~298), replace the bold text next to the "KYC" pill in the collapsed header from `{customer.profile.compliance.kycStatus}` (currently renders "Current") to the literal string **"Anonymized · No PII"**.

No other changes.
