Replace all instances of `ventus.ai` with `ventusai.com` across the codebase.

## Files to change
1. `src/components/tepilot/advisor-console/AdvisorNotificationsView.tsx` — line 132
   - Change `<copilot@ventus.ai>` to `<copilot@ventusai.com>`

2. `src/components/demo/DemoEngineProfileView.tsx` — line 287
   - Change `ventus.ai/profile/{customer.id}` to `ventusai.com/profile/{customer.id}`

## Verification
Run a search to confirm no `ventus.ai` references remain after edits.