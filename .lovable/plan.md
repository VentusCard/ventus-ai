## Changes

In `src/components/demo/DemoNetworkDiagram.tsx`, update the first pillar row (lines 49-50):

1. `**team**`: `"Analytics"` → `"Digital Experience"`
2. `**subtitle**`: `"Where do our customers spend their money?"` → `"How can we help our customers understand their spending?"`
3. `update the "personalized UX" node name to "Personalized AI & UX"`

No other files need changes.