

## Rename "Frequent Traveler" → "Leisure Traveler"

One-word change in the persona label so both the pill inside the card and the floating callout bubble read **"Leisure Traveler"**. Better captures the diversified evidence (Rimowa, Global Entry, Viator tours) — this isn't a road-warrior, it's someone spending on travel as a lifestyle.

### Files touched

- `src/components/ScrollDrivenHero.tsx` — single string change in the `personas` array (`label: "Frequent Traveler"` → `label: "Leisure Traveler"`). The callout bubble reads from the same `label` field, so it updates automatically.

No other changes.

