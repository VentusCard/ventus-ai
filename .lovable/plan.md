Remove the "Talking Points" section from `src/components/exec-demo/WMCopilotPhoneView.tsx` (the heading at line 64 and the surrounding wrapper that maps `brief.talkingPoints`). Leave the rest of the WM Copilot phone view (header, AI prompt, Tasks Automated file card, etc.) intact.