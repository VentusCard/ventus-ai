

## What changed

User wants to use the **deduped persona rollups** (from `synthesize-persona`) as the source for deal generation. No `pillar` field needed — just rollup label + its related transactions.

## Investigation

Let me read the current state of the persona output, the page wiring, and the edge function.
